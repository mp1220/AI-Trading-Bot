"""Application package initialization for the trading bot."""

# Expose key submodules for convenient imports when running as a package.
# This keeps imports like `from app import config` working consistently.
